#include"main.h"

/**
 * abs - function that computes the absolute
 *
 * takes integer type input
 *
 *
 * Return: Always 0 (Success)
 */

int _abs(int n)
{
	if (n < 0)
		n = (-1) * n;
	return (n);
}
